({
	navigateTo: function (url) {
		console.warn('helper', url)

		/*var urlEvent = $A.get("e.force:navigateToURL");
		
		urlEvent.setParams({
			"url": "/profile-settings"
		});
		urlEvent.fire();*/
	}
})