({
	onInit : function(cmp, evt, hlpr) {
		// console.info(cmp.get("v.accountGrandTotal").length);
		// if(cmp.get("v.accountGrandTotal").length === 0) {
		// 	cmp.set("v.class", "igcp-overview--account slds-size_12-of-12");
		// }
	}
})