<aura:component >

	<!-- ATTRS -->
	<aura:attribute name="container" type="String" default=""></aura:attribute>

	<!-- HANDLERS -->
	<aura:handler name="init" value="{!this}" action="{!c.onInit}" />

	<!-- MARKUP -->
    <a class="igcp-link__print igcp-label--small slds-size_12-of-12 slds-m-bottom--x-small" onclick="{!c.printWindow}">
    	<lightning:icon iconName="utility:record" size="small" class="igcp-link__print--icon slds-m-right--xxx-small"/>{!$Label.c.CP_Generic_Label_Print}
   	</a>
</aura:component>