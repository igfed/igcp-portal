({
    
    doInit: function(cmp, evt, hlpr) {
         
        


    },
    onCheck: function(cmp, evt) {
         var isChecked = cmp.get("v.isChecked");
         console.log(isChecked);
        
	 }
})